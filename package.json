{
  "main": "dist/index.js",
  "scripts": {
    "build": "bun build src/index.ts --outdir=dist --minify --target=node"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.1"
  }
}